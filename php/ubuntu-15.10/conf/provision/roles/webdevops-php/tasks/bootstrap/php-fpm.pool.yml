---

- name: Set php pool file
  set_fact:
     php_pool_conf: /etc/php-fpm.d/www.conf
  when: ansible_os_family == 'RedHat'

- name: Set php pool file
  set_fact:
     php_pool_conf: /etc/php5/fpm/pool.d/www.conf
  when: ansible_os_family == 'Debian'

- name: Create php directories
  file:
    path:  "{{ item }}"
    state: directory
    mode:  0755
    owner: "root"
    group: "root"
  with_items:
    - /opt/docker/etc/php/fpm/
    - /opt/docker/etc/php/fpm/pool.d/

- name: Move php-fpm main file to /opt/docker/etc/php/
  command: "mv {{ php_pool_conf }} /opt/docker/etc/php/fpm/pool.d/application.conf"

- name: Symlink php-fpm main file
  file:
    path: "{{ php_pool_conf }}"
    src:  "/opt/docker/etc/php/fpm/pool.d/application.conf"
    state: link
    owner: "root"
    group: "root"

- name: Configure php-fpm pool user (www.conf)
  lineinfile:
    dest:   '/opt/docker/etc/php/fpm/pool.d/application.conf'
    regexp: '^[\s;]*{{ item.key }}[\s]*='
    line:   '{{ item.key }} = {{ item.value }}'
  with_items:
   - { key: 'listen',                      value: "0.0.0.0:9000" }
   - { key: 'catch_workers_output',        value: "yes" }
   - { key: 'access.log',                  value: "/var/log/php5-fpm/access.log" }
   - { key: 'slowlog',                     value: "/var/log/php5-fpm/slow.log" }
   - { key: 'php_admin_value[error_log]',  value: "/var/log/php5-fpm/error.log" }
   - { key: 'php_admin_value[log_errors]', value: "on" }
   - { key: 'user',                        value: "{{ APPLICATION_USER }}" }
   - { key: 'group',                       value: "{{ APPLICATION_GROUP }}" }

- name: Disable php-fpm connection limit
  lineinfile:
    dest:   '/opt/docker/etc/php/fpm/pool.d/application.conf'
    regexp: '^[\s;]*{{ item.key }}[\s]*='
    line:   ';{{ item.key }}'
  with_items:
   - { key: 'listen.allowed_clients' }

