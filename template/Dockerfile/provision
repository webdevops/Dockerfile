{% macro runRoleInline() %}
    {%- if varargs -%}&& /opt/docker/bin/provision add --tag bootstrap {% for role in varargs -%}
        {% if role %}webdevops-{{ role }} {% endif %}
        {%- endfor -%}
    \{%- endif %}
    && /opt/docker/bin/bootstrap.sh
{% endmacro %}
