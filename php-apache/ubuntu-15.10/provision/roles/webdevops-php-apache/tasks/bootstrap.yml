---

- name: Restrict php-fpm to local connection
  lineinfile:
    dest:   /etc/php5/fpm/pool.d/www.conf
    regexp: '^[\s;]*{{ item.key }}[\s]*='
    line:   '{{ item.key }} = {{ item.value }}'
  with_items:
   - { key: 'listen', value: "/var/run/php5-fpm.sock" }
