{{ docker.from("base-app", "centos-7") }}

{{ docker.copy('conf/', '/opt/docker/') }}

RUN set -x \
    {{ php7.centosWebtatic() }} \
    {{ provision.runBootstrap() }} \
    {{ docker.cleanup() }}

{{ docker.expose('9000') }}
